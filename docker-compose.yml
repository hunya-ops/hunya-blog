services:
  web:
    build: .
    ports:
      - "${PORT:-5000}:5000"
    volumes:
      - ./data:/app/data
      - ./app/static/uploads:/app/app/static/uploads
      - ./.env:/app/.env
    restart: unless-stopped
