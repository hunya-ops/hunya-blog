services:
  web:
    build: .
    ports:
      - "${DOCKER_BIND:-0.0.0.0}:${PORT:-5000}:5000"
    volumes:
      - ./data:/app/data
      - ./app/static/uploads:/app/app/static/uploads
      - ./env:/app/.env
    restart: unless-stopped
